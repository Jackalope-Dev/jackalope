import { DEFAULT_THEME, themeTokens } from '@jackalope/brand/tokens';

const colors = (isDark: boolean) =>
  Object.entries(themeTokens({ ...DEFAULT_THEME, isDark, appearance: 'manual' }))
    .map(([key, value]) => `${key}:${value};`)
    .join('');

export function accessAdminPage(nonce: string) {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Early access · Jackalope admin</title><style nonce="${nonce}">
:root{${colors(false)}}@media(prefers-color-scheme:dark){:root{${colors(true)}}}
.insight-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:24px}.insight-grid .facts{display:grid;grid-template-columns:1fr auto}.insight-grid dd{margin:0;font-variant-numeric:tabular-nums}*{box-sizing:border-box}body{margin:0;background:var(--color-surface);color:var(--color-text-primary);font:15px/1.6 system-ui}main{max-width:1120px;margin:auto;padding:32px 24px 64px}a{color:var(--color-accent-ink);text-underline-offset:4px}h1{font-size:clamp(28px,5vw,40px);letter-spacing:-.04em;margin:28px 0 8px}h2{font-size:20px;margin:0 0 8px;letter-spacing:-.02em}h3{font-size:16px;margin:0}p{margin:8px 0;color:var(--color-text-secondary)}small{font-size:13px;color:var(--color-text-secondary)}nav,.actions,.filters,.tabs{display:flex;gap:12px;flex-wrap:wrap;align-items:center}nav{justify-content:space-between}button,input,select{font:inherit;color:inherit;min-height:44px;padding:9px 14px;border:1px solid var(--color-border);border-radius:8px;background:var(--color-surface-elevated)}button{cursor:pointer}button:hover{background:var(--color-surface-hover)}button.primary{background:var(--color-accent);color:var(--color-on-accent);border-color:transparent}button:disabled{opacity:.55;cursor:not-allowed}:focus-visible{outline:2px solid var(--color-accent-ink);outline-offset:3px}label{display:grid;gap:5px}input{min-width:0}.readiness{margin:20px 0 24px;padding:24px;border:1px solid var(--color-border);border-radius:12px;background:var(--color-surface-elevated)}.readiness-heading{display:flex;justify-content:space-between;align-items:start;gap:20px}.readiness-heading button{flex-shrink:0}.checks{margin:16px 0;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:24px;padding:0;list-style:none}.checks li{display:grid;align-content:start;gap:4px}.checks strong{font-weight:600}.warning{color:var(--color-warning)}.checks small{display:block}details{margin-top:16px}summary{cursor:pointer;min-height:44px;padding:10px 0;color:var(--color-accent-ink)}details ol{padding-left:22px}details li{margin-bottom:8px}#people-heading{margin-top:32px}.tabs{gap:6px;margin:20px 0}.tabs button[aria-pressed=true]{background:var(--color-accent-subtle);color:var(--color-accent-ink);border-color:var(--color-accent-ink)}.filters{align-items:flex-end;margin:16px 0}.filters label{flex:1;max-width:440px}.filters input{width:100%}.person{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:24px;align-items:center;padding:24px 0;border-top:1px solid var(--color-border)}.person p{font-size:13px}.person h3{overflow-wrap:anywhere}.status{display:inline-block;color:var(--color-accent-ink);font-size:13px;font-weight:600}.notice{min-height:24px}.empty{padding:32px 0}.email-entry{padding:18px 0;border-top:1px solid var(--color-border)}.email-entry .actions{justify-content:space-between}dialog{color:var(--color-text-primary);background:var(--color-surface);border:1px solid var(--color-border);border-radius:14px;padding:28px;width:620px;max-width:calc(100vw - 32px);max-height:calc(100dvh - 48px);overflow:auto;box-shadow:var(--shadow-pop)}dialog::backdrop{background:rgb(0 0 0 / .55)}dialog header{display:flex;justify-content:space-between;align-items:start;gap:16px}dialog h2,dialog p{overflow-wrap:anywhere}.facts{display:grid;grid-template-columns:150px 1fr;gap:8px 20px;margin:20px 0}.facts dt{color:var(--color-text-secondary)}.facts dd{margin:0;overflow-wrap:anywhere}.confirmation{display:flex;align-items:start;gap:12px;margin:20px 0}.confirmation input{width:20px;min-height:20px;margin-top:3px;flex-shrink:0}.confirmation span{flex:1}dialog .footer{margin-top:24px}.compose{display:grid;gap:16px;margin:20px 0}.compose h3{margin-top:8px}.compose textarea{font:inherit;color:inherit;width:100%;padding:10px 14px;border:1px solid var(--color-border);border-radius:8px;background:var(--color-surface-elevated);resize:vertical}.entries{display:grid;gap:2px;max-height:340px;overflow:auto;padding:4px;border:1px solid var(--color-border);border-radius:10px}.entry{display:grid;grid-template-columns:22px 1fr;gap:12px;align-items:start;padding:12px;border-radius:8px}.entry:hover{background:var(--color-surface-hover)}.entry input{width:20px;min-height:20px;margin-top:3px}.entry strong{font-weight:600}.entry small{display:block;color:var(--color-text-secondary)}.extra{display:grid;gap:8px;padding:14px;margin-bottom:12px;border:1px solid var(--color-border);border-radius:10px}.extra .actions{justify-content:flex-end}
[hidden]{display:none!important}@media(max-width:700px){.readiness-heading{flex-direction:column;gap:8px}.checks{grid-template-columns:1fr;gap:16px}.person{grid-template-columns:1fr;gap:12px}.checks li{grid-template-columns:1fr;gap:2px}.filters label{max-width:none}.filters>*{width:100%}.facts{grid-template-columns:1fr;gap:2px}.facts dd{margin-bottom:12px}dialog{padding:20px}.readiness{padding:20px}.tabs{gap:8px}main{padding:24px 20px}}
</style></head><body><main>
<nav aria-label="Admin navigation"><strong>Jackalope admin</strong><a href="/admin">Usage & feedback</a></nav>
<header><h1>Early access</h1><p>Review requests, invite one person at a time, and follow their first sign-in.</p></header>
<section class="readiness" aria-labelledby="ready-heading"><div class="readiness-heading"><div><h2 id="ready-heading">Before you invite</h2><p id="ready-summary" role="status">Checking the service and private download…</p></div><button id="refresh-ready">Recheck setup</button></div><ul id="checks" class="checks"></ul>
<details><summary>First-user onboarding checklist</summary><ol>
<li>Configure the certified Microsoft Store listing link, or upload a reviewed, publisher-signed Windows installer and configure its private download key.</li>
<li>Use your own approved account to test the email, sign-in and download. Install in a clean Windows profile; create a project, connect an agent, finish a small task and review its changes.</li>
<li>Check the published privacy notice, terms and monitored support address. Confirm the desktop account connection, restart and disconnect flows if this build includes them.</li>
<li>Approve the first person when you can help them. Ask them to complete one small task and tell you where they get stuck.</li>
</ol><p>Setup checks below show configuration and file availability. They do not verify the publisher signature, installation, inbox placement or a completed user task.</p></details>
</section>
<section class="readiness" aria-labelledby="insights-heading"><div class="readiness-heading"><h2 id="insights-heading">What future users need</h2><button id="refresh-insights">Refresh insights</button></div><p id="insight-summary" role="status"></p><div id="insights" class="insight-grid"></div><p>Optional answers, multiple choices per person. These describe respondents, not all visitors. Missing answers are not zero demand.</p></section><h2 id="people-heading">People</h2><p>Waitlist referrals are unlimited. Each verified signup earns one day of priority. Instant Access Passes are a separate allowance after acceptance.</p><div id="tabs" class="tabs" role="group" aria-label="Filter people"></div>
<div class="filters"><label>Order<select id="sort" aria-label="Order"><option value="priority">Waitlist priority</option><option value="referrals">Most referrals</option><option value="newest">Newest first</option></select></label><label>Find an email<input id="query" type="search" maxlength="254" placeholder="Search in the selected group"></label><label>Platform<select id="platform" aria-label="Platform"><option value="all">All platforms</option><option value="macos">macOS</option><option value="windows">Windows</option><option value="linux">Linux</option></select></label><label>Onboarding stage<select id="stage" aria-label="Onboarding stage"><option value="all">All stages</option><option value="not-signed-in">Approved, not signed in</option><option value="not-connected">Signed in, no desktop yet</option><option value="connected">Connected a desktop</option><option value="email-failed">Email needs attention</option></select></label><button id="search">Search</button><button id="refresh">Refresh people</button></div>
<p id="notice" class="notice" role="status" aria-live="polite"></p><p id="error" role="alert" hidden></p><p id="results" class="notice"></p><div id="members" aria-labelledby="people-heading"></div><button id="more" hidden>Load more people</button>
<section class="readiness" aria-labelledby="notes-heading"><div class="readiness-heading"><div><h2 id="notes-heading">Product notes</h2><p id="notes-summary" role="status">Loading published changes…</p></div><button id="refresh-notes">Reload changes</button></div>
<div class="compose">
<label>Subject<input id="note-subject" maxlength="150" placeholder="What shipped in Jackalope this month"></label>
<label>Preview text<input id="note-preview" maxlength="150" placeholder="Shown after the subject in the inbox"></label>
<label>Headline<input id="note-headline" maxlength="120" placeholder="A few things worth knowing."></label>
<label>Opening<textarea id="note-intro" rows="3" maxlength="1200" placeholder="Your own words. Blank lines start a new paragraph."></textarea></label>
<h3 id="entries-heading">Include these changes</h3>
<p>Straight from the published changelog. Nothing is included until you tick it.</p>
<div id="note-entries" role="group" aria-labelledby="entries-heading" class="entries"></div>
<h3>Add something of your own</h3>
<div id="note-extras"></div><div class="actions"><button id="add-extra">Add a section</button></div>
<label>Closing<textarea id="note-outro" rows="3" maxlength="1200" placeholder="Sign-off, an ask, or what is coming next."></textarea></label>
<div class="filters"><label>Button label<input id="note-action-label" maxlength="60" placeholder="Optional"></label><label>Button link<input id="note-action-url" type="url" maxlength="300" placeholder="https://jackalope.dev/…"></label></div>
<label>Who receives it<select id="note-tag"><option value="">Everyone who opted into product notes</option></select></label>
<p id="note-audience" class="notice"></p>
</div>
<p id="note-error" role="alert" hidden></p><p id="note-result" class="notice" role="status"></p>
<div class="actions"><button id="note-preview-open">Preview in a new tab</button><button id="note-draft" class="primary">Create draft in Sequenzy</button></div>
<p><small>Creating a draft never sends email. The note lands in Sequenzy with its audience and sender set, and you press send there after reading it.</small></p>
</section>
<details><summary>Email previews</summary><nav aria-label="Email previews"><a href="/admin/access/email-preview?kind=waitlist" target="_blank" rel="noreferrer">Waitlist verification</a><a href="/admin/access/email-preview?kind=invite" target="_blank" rel="noreferrer">Instant Access Pass</a><a href="/admin/access/email-preview?kind=referral" target="_blank" rel="noreferrer">Referral milestone</a><a href="/admin/access/email-preview?kind=passes_ready" target="_blank" rel="noreferrer">Passes ready</a><a href="/admin/access/email-preview?kind=pass_claimed" target="_blank" rel="noreferrer">Pass claimed</a><a href="/admin/access/email-preview?kind=pass_expired" target="_blank" rel="noreferrer">Pass expired</a><a href="/admin/access/email-preview" target="_blank" rel="noreferrer">Approval email</a><a href="/admin/access/email-preview?kind=login" target="_blank" rel="noreferrer">Sign-in email</a></nav><p>Previews use sample data and cannot sign anyone in.</p></details>
</main>
<dialog id="detail" aria-labelledby="detail-title"><header><h2 id="detail-title">Person details</h2><button id="close-detail" aria-label="Close person details">Close</button></header><div id="detail-body"></div><p id="detail-error" role="alert"></p></dialog>
<dialog id="confirm" aria-labelledby="confirm-title"><h2 id="confirm-title"></h2><p id="confirm-email"></p><p id="confirm-copy"></p><label id="download-warning" class="confirmation" hidden><input id="acknowledge" type="checkbox"><span>No download is available or it could not be checked. I understand this person may see “No download available yet.”</span></label><p id="confirm-error" role="alert"></p><div class="actions footer"><button id="confirm-action" class="primary"></button><button id="cancel-action">Cancel</button></div></dialog>
<script nonce="${nonce}">
const $=id=>document.getElementById(id);
const groupNames={waiting:'Waiting',approved:'Approved',revoked:'Revoked',all:'Everyone'};
const mailNames={feedback_request:'Feedback invitation',waitlist:'Waitlist confirmation',welcome:'Approval email',login:'Sign-in email',invite:'Instant Access Pass',referral:'Referral milestone',passes_ready:'Passes ready',pass_claimed:'Pass claimed',pass_expired:'Pass reservation expired'};
const deliveryNames={pending:'Waiting at the email service',queued:'Queued at the email service',sent:'Sent by the email service',delivered:'Accepted by the recipient’s mail server',opened:'Provider recorded an open',clicked:'Provider recorded a click',bounced:'Bounced — check the email address',complained:'Recipient reported spam',suppressed:'Suppressed by the email service',failed:'Delivery failed — check Sequenzy',deferred:'Delivery delayed',unknown:'Delivery status not recognized'};
const errors={origin_required:'Refresh your private admin sign-in and try again.',access_not_configured:'Email or early access is not configured. Recheck setup.',member_changed:'This person’s access changed. Close this dialog and refresh people.',download_not_ready:'The download is no longer ready. Close this dialog and recheck setup before approving.',delivery_unavailable:'This older email has no delivery reference. Check its history in Sequenzy.',delivery_check_failed:'Could not check delivery. The last known status is unchanged; retry or check Sequenzy.',mail_not_configured:'The email service is not configured.',mail_not_found:'This email is no longer in the retained history.',changelog_unavailable:'Could not read the published changelog from the website. Check that the site is deployed, then reload changes.',broadcast_rejected:'Sequenzy rejected the draft. Check the sender domain and audience list, then try again.'};
let group='waiting',cursor=null,epoch=0,controller,ready=null,action=null,actionBusy=false,detailEpoch=0;
function node(tag,text,className){const element=document.createElement(tag);if(text!==undefined)element.textContent=text;if(className)element.className=className;return element;}
function date(value){return value?new Date(value).toLocaleString():'Not yet';}
async function api(path,options={}){const response=await fetch(path,{credentials:'same-origin',redirect:'error',...options});if(!response.headers.get('content-type')?.includes('application/json'))throw Error('Your admin session may have expired. Refresh your private sign-in.');const data=await response.json();if(!response.ok)throw Error(errors[data.error]||(response.status===403?'Your admin session may have expired. Refresh your private sign-in.':'Could not complete the request. Try again.'));return data;}
function fail(id,error){$(id).textContent=error.message||'Could not complete the request. Try again.';$(id).hidden=false;}
function setTabs(counts=[]){const focused=document.activeElement?.dataset.group;const totals=Object.fromEntries(counts.map(row=>[row.status,row.count]));totals.all=counts.reduce((sum,row)=>sum+row.count,0);$('tabs').replaceChildren();for(const [value,name] of Object.entries(groupNames)){const button=node('button',name+' ('+(totals[value]??0)+')');button.dataset.group=value;button.setAttribute('aria-pressed',String(value===group));button.onclick=()=>{group=value;setTabs(counts);void load();};$('tabs').append(button);if(focused===value)button.focus();}}
function mailStatus(mail){if(mail.delivery_status)return deliveryNames[mail.delivery_status]||deliveryNames.unknown;const state=mail.state??mail.mail_state;if(state==='queued')return 'Accepted by email service · delivery not checked';if(state==='sending')return 'Handing off to email service';if(state==='failed')return (mail.attempts??mail.mail_attempts)>=5?'Could not hand off email · retries exhausted':'Handoff failed · automatic retry pending';return state==='pending'?'Waiting to send':'No email recorded';}
async function loadReadiness(){ $('refresh-ready').disabled=true;try{ready=await api('/admin/api/access/readiness');$('checks').replaceChildren();const checks=[['Early access',ready.enabled?'Enabled':'Disabled',ready.enabled?'Requests can be approved.':'Enable early access in the service configuration.'],['Email service',ready.mailConfigured?'Configured':'Needs setup',ready.mailConfigured?'Confirm real inbox delivery with your own account.':'Configure the sender and email service credentials.'],[ready.distribution==='store'?'Microsoft Store':'Private download',ready.distribution==='store'?'Link configured':ready.download==='available'?'Available · '+ready.version:'Not ready',ready.distribution==='store'?'Verify the listing opens for a tester and complete Store installation before inviting users.':ready.download==='available'?'File exists. Validate its signature and installation before inviting users.':ready.download==='missing'?'The configured installer is missing or empty.':ready.download==='unknown'?'Could not read the installer. Check storage and retry.':'Configure ACCESS_STORE_URL after accepting the Store listing, or upload a reviewed private installer.']];for(const [name,status,description] of checks){const row=node('li');row.append(node('span',name));const info=node('div');info.append(node('strong',status),node('small',description));row.append(info);$('checks').append(row);}$('ready-summary').textContent=ready.enabled&&ready.mailConfigured&&ready.download==='available'?'The service and download are configured. Complete the first-user trial before approving someone.':'Finish setup before inviting someone to use the app.';}catch(error){ready=null;$('checks').replaceChildren();fail('ready-summary',error);}finally{$('refresh-ready').disabled=false;}}
function progress(person){return person.status==='revoked'?'Access revoked':person.status==='waiting'?'Waiting for approval':person.devices>0?'Desktop connected':person.verified_at?'Signed in to website':'Approved · awaiting first sign-in';}
function rowFor(person){const row=node('article',undefined,'person');const info=node('div');info.append(node('h3',person.email),node('span',progress(person),'status'),node('p','Requested '+date(person.created_at)),node('p',(mailNames[person.mail_kind]||'Email')+': '+mailStatus(person)));info.append(node('p',(person.position?'#'+person.position+' in line · ':'')+person.referral_count+' verified referrals · '+person.pending_referrals+' awaiting verification · '+(person.accepted||0)+' passes claimed'));if(person.preferences){const preferences=JSON.parse(person.preferences);info.append(node('p',[...preferences.platforms,...preferences.agents].join(' · ')));}const actions=node('div',undefined,'actions');const details=node('button','Details');details.onclick=()=>void showDetails(person);actions.append(details);const choices=person.status==='waiting'?[['approve','Approve & email'],['revoke','Remove from waitlist']]:person.status==='approved'?[['resend','Send access link'],['revoke','Revoke access']]:[['restore','Return to waitlist']];for(const [kind,label]of choices){const button=node('button',label,kind==='approve'?'primary':undefined);button.onclick=()=>confirmAction(person,kind,label);actions.append(button);}row.append(info,actions);return row;}
async function load(append=false){const current=++epoch;controller?.abort();controller=new AbortController();$('members').inert=true;$('more').disabled=true;$('notice').textContent='Loading people…';$('error').hidden=true;try{const params=new URLSearchParams({status:group,query:$('query').value,platform:$('platform').value,stage:$('stage').value,sort:$('sort').value});if(append)params.set('offset',String($('members').children.length));const data=await api('/admin/api/access?'+params,{signal:controller.signal});if(current!==epoch)return;if(!append)$('members').replaceChildren();setTabs(data.counts);for(const person of data.members)$('members').append(rowFor(person));cursor=data.members.at(-1)?.cursor;$('more').hidden=!data.hasMore;const shown=$('members').children.length;$('results').textContent=shown?'Showing '+shown+' '+(shown===1?'person':'people')+' · '+$('sort').selectedOptions[0].textContent.toLowerCase():'';$('notice').textContent=shown?'':'No people match this view. Try another group or clear the search.';}catch(error){if(current!==epoch)return;$('members').replaceChildren();$('results').textContent='';$('more').hidden=true;$('notice').textContent='';fail('error',error);}finally{if(current===epoch){$('members').inert=false;$('more').disabled=false;}}}
async function loadInsights(){ $('refresh-insights').disabled=true; $('insight-summary').textContent='Loading preferences…'; try{const data=await api('/admin/api/access/insights');$('insights').replaceChildren();$('insight-summary').textContent=data.totals.responded+' of '+data.totals.requested+' people answered · '+(data.totals.approved||0)+' approved · '+(data.totals.connected||0)+' connected a desktop';for(const [key,title] of [['platforms','Operating systems'],['agents','Preferred agents'],['priorities','Workflow priorities'],['sources','Signup sources']]){const section=node('section');section.append(node('h3',title));const list=node('dl',undefined,'facts');for(const row of data[key])fact(list,row.label,String(row.count));section.append(list);if(!data[key].length)section.append(node('p','No responses yet'));$('insights').append(section);}}catch(error){$('insights').replaceChildren();fail('insight-summary',error);}finally{$('refresh-insights').disabled=false;}}
function fact(list,label,value){list.append(node('dt',label),node('dd',value));}
async function showDetails(person){const current=++detailEpoch;$('detail-title').textContent=person.email;$('detail-body').replaceChildren(node('p','Loading details…'));$('detail-error').textContent='';$('detail').showModal();try{const data=await api('/admin/api/access/member?id='+encodeURIComponent(person.id));if(current!==detailEpoch)return;const m=data.member;const facts=node('dl',undefined,'facts');fact(facts,'Access',groupNames[m.status]);if(m.preferences){const prefs=JSON.parse(m.preferences);for(const [key,label] of [['platforms','Operating systems'],['agents','Preferred agents'],['priorities','Priorities']])fact(facts,label,prefs[key].join(', ')||'Not answered');}fact(facts,'Waitlist referrals',m.referral_count+' verified · '+m.pending_referrals+' awaiting verification');fact(facts,'Waitlist verified',date(m.waitlist_verified_at));fact(facts,'Campaign',m.campaign||'Not recorded');fact(facts,'Requested',date(m.created_at));fact(facts,'Approved',date(m.approved_at));fact(facts,'First website sign-in',date(m.verified_at));fact(facts,'Connected desktops',String(data.devices.length));fact(facts,'Instant Access Passes',data.invites.reduce((n,i)=>n+(i.status==='accepted'?i.count:0),0)+' accepted · '+data.invites.reduce((n,i)=>n+(i.status==='pending'?i.count:0),0)+' pending · '+m.invite_limit+' allowance');fact(facts,'Product notes',m.newsletter?(m.newsletter_synced_at?'Opted in · synced':m.newsletter_attempts>=5?'Opted in · sync needs attention':'Opted in · sync pending'):'Not opted in');$('detail-body').replaceChildren(facts,node('p','Website sign-in and desktop connection do not establish that a task was completed.'),node('h3','Recent emails'),node('p','Up to 10 retained messages. Mail-server acceptance does not confirm inbox placement.'));if(data.feedback){const f=data.feedback;const section=node('section');section.append(node('h3','Feedback follow-up'),node('p',f.completed_at?'Feedback received; this round is complete.':f.enabled?'Email follow-up enabled by this member.':'Feedback emails are off.'),node('p','Shared activity: '+f.active_days+' of 2 active days; '+f.opened_results+' of 2 opened results. These milestones do not prove satisfaction.'),node('p','In-app invitations: '+f.prompt_count+' of 2. Email invitation: '+(f.email_requested?'requested':'not requested')+'.'),node('p','Next eligible request: '+date(f.next_prompt_at)));$('detail-body').append(section);}if(!data.mail.length)$('detail-body').append(node('p','No email history retained.'));for(const mail of data.mail){const entry=node('section',undefined,'email-entry');entry.append(node('strong',mailNames[mail.kind]||'Account email'),node('p',date(mail.created_at)));const status=node('p',mailStatus(mail));const checked=node('small',mail.delivery_checked_at?'Last checked '+date(mail.delivery_checked_at):'');entry.append(status,checked);if(mail.can_check){const button=node('button','Check delivery');button.onclick=async()=>{button.disabled=true;$('detail-error').textContent='';try{const value=await api('/admin/api/access/delivery',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id:mail.id})});status.textContent=deliveryNames[value.status]||deliveryNames.unknown;checked.textContent='Last checked '+date(value.checkedAt);}catch(error){fail('detail-error',error);}finally{button.disabled=false;if($('detail').open&&current===detailEpoch)button.focus();}};const actions=node('div',undefined,'actions');actions.append(button);entry.append(actions);}else if(mail.state==='queued')entry.append(node('p','Delivery lookup is unavailable for this older message. Check Sequenzy.'));$('detail-body').append(entry);}}catch(error){if(current===detailEpoch){$('detail-body').replaceChildren();fail('detail-error',error);}}}
function confirmAction(person,kind,label){if(actionBusy)return;action={person,kind};$('confirm-title').textContent=label+'?';$('confirm-email').textContent=person.email;const copy={approve:'This grants early access and queues one approval email with a private link. Their Instant Access Pass allowance is '+person.invite_limit+'.',resend:'This queues a new sign-in link. It expires in 30 minutes. A one-minute cooldown and daily limit prevent repeated emails.',revoke:'This ends website sessions and desktop account access, and cancels pending invitations. Local projects and already accepted invitees keep their own access.',restore:'This returns the person to the waitlist. It sends no email and does not restore old sign-in links or desktop credentials.'};$('confirm-copy').textContent=copy[kind];$('confirm-action').textContent=label;$('confirm-error').textContent='';$('acknowledge').checked=false;$('download-warning').hidden=kind!=='approve'||ready?.download==='available';$('confirm-action').disabled=!$('download-warning').hidden;if((kind==='approve'||kind==='resend')&&(!ready?.enabled||!ready?.mailConfigured)){$('confirm-error').textContent='Recheck setup and configure early access and email before sending.';$('confirm-action').disabled=true;}$('confirm').showModal();}
$('acknowledge').onchange=()=>{$('confirm-action').disabled=!$('acknowledge').checked||!ready?.enabled||!ready?.mailConfigured;};
$('confirm-action').onclick=async()=>{if(actionBusy||!action)return;actionBusy=true;$('confirm-action').disabled=true;$('cancel-action').disabled=true;$('confirm-error').textContent='';const {person,kind}=action;try{const result=await api('/admin/api/access',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({id:person.id,action:kind,allowWithoutDownload:kind==='approve'&&$('acknowledge').checked})});$('confirm').close();await load();$('notice').textContent=(kind==='approve'?'Approved '+person.email+'. ':kind==='resend'?'Access link for '+person.email+': ':'Access updated for '+person.email+'. ')+((kind==='approve'||kind==='resend')?(result.mailQueued?'Email added to the outbox. Delivery is not confirmed yet.':'No new email was queued. A recent request or daily limit may apply; check Details before retrying.'):'');$('query').focus();}catch(error){fail('confirm-error',error);}finally{actionBusy=false;$('cancel-action').disabled=false;$('confirm-action').disabled=!$('download-warning').hidden&&!$('acknowledge').checked;}};
$('cancel-action').onclick=()=>$('confirm').close();$('confirm').addEventListener('cancel',event=>{if(actionBusy)event.preventDefault();});$('close-detail').onclick=()=>$('detail').close();$('detail').addEventListener('close',()=>detailEpoch++);
$('sort').onchange=()=>void load();$('platform').onchange=()=>void load();$('stage').onchange=()=>{group='all';void load();};$('search').onclick=()=>void load();$('query').onkeydown=event=>{if(event.key==='Enter')void load();};$('refresh').onclick=()=>void load();$('more').onclick=()=>void load(true);$('refresh-ready').onclick=()=>void loadReadiness();

function extraRow(){const row=node('div',undefined,'extra');const title=node('label','Section title');const titleInput=document.createElement('input');titleInput.maxLength=200;titleInput.className='extra-title';title.append(titleInput);const body=node('label','Section text');const bodyInput=document.createElement('textarea');bodyInput.rows=3;bodyInput.maxLength=800;bodyInput.className='extra-body';body.append(bodyInput);const actions=node('div',undefined,'actions');const remove=node('button','Remove section');remove.onclick=()=>{row.remove();$('add-extra').focus();};actions.append(remove);row.append(title,body,actions);return row;}
function draftNote(){const extras=[...document.querySelectorAll('.extra')].map(row=>({title:row.querySelector('.extra-title').value.trim(),body:row.querySelector('.extra-body').value.trim()})).filter(extra=>extra.title||extra.body);const url=$('note-action-url').value.trim();return{subject:$('note-subject').value.trim(),preview:$('note-preview').value.trim(),headline:$('note-headline').value.trim(),intro:$('note-intro').value.trim(),outro:$('note-outro').value.trim(),entries:[...document.querySelectorAll('#note-entries input:checked')].map(input=>input.value),extras,action:url?{label:$('note-action-label').value.trim()||'Read more',url}:null,tag:$('note-tag').value};}
function noteReady(){const note=draftNote();if(note.subject.length<3)return'Give the note a subject before previewing it.';if(note.headline.length<3)return'Give the note a headline before previewing it.';if(!note.entries.length&&!note.extras.length&&!note.intro)return'Choose at least one change, or write an opening.';if(note.action&&!/^https:\\/\\//.test(note.action.url))return'The button link must be an https:// URL.';return'';}
function previewUrl(){const note=draftNote();const params=new URLSearchParams({subject:note.subject,preview:note.preview,headline:note.headline,intro:note.intro,outro:note.outro,tag:note.tag});for(const id of note.entries)params.append('entry',id);for(const extra of note.extras)params.append('extra',extra.title+'\\n'+extra.body);if(note.action){params.set('actionLabel',note.action.label);params.set('actionUrl',note.action.url);}return'/admin/access/broadcast-preview?'+params;}
async function loadNotes(){$('refresh-notes').disabled=true;$('note-error').hidden=true;try{const data=await api('/admin/api/access/broadcast');$('note-entries').replaceChildren();for(const entry of data.entries){const row=node('label',undefined,'entry');const box=document.createElement('input');box.type='checkbox';box.value=entry.id;const info=node('div');info.append(node('strong',entry.title),node('small',entry.date+(entry.status?' · '+entry.status:'')),node('small',entry.description));row.append(box,info);$('note-entries').append(row);}
if(!data.entries.length)$('note-entries').append(node('p','No published changes yet.','empty'));
$('note-tag').replaceChildren();const all=document.createElement('option');all.value='';all.textContent='Everyone who opted into product notes';$('note-tag').append(all);for(const tag of data.tags){const option=document.createElement('option');option.value=tag;option.textContent='Only '+tag.replace(/-/g,' ');$('note-tag').append(option);}
$('notes-summary').textContent=data.entries.length+' published changes to choose from';$('note-audience').textContent=data.configured?data.synced+' people are synced to the audience and can receive this.':'Sequenzy, the audience list or the sender is not configured, so a draft cannot be created yet.';$('note-draft').disabled=!data.configured;}catch(error){$('note-entries').replaceChildren();fail('notes-summary',error);}finally{$('refresh-notes').disabled=false;}}
$('add-extra').onclick=()=>{const row=extraRow();$('note-extras').append(row);row.querySelector('input').focus();};
$('note-preview-open').onclick=()=>{const problem=noteReady();$('note-error').hidden=!problem;$('note-error').textContent=problem;if(!problem)window.open(previewUrl(),'_blank','noreferrer');};
$('note-draft').onclick=async()=>{const problem=noteReady();$('note-error').hidden=!problem;$('note-error').textContent=problem;if(problem)return;$('note-draft').disabled=true;$('note-result').textContent='Creating the draft…';try{const result=await api('/admin/api/access/broadcast',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(draftNote())});$('note-result').replaceChildren(document.createTextNode('Draft created in Sequenzy. Nothing has been sent. '));if(result.url){const link=node('a','Open it to review and send');link.href=result.url;link.target='_blank';link.rel='noreferrer';$('note-result').append(link);}}catch(error){$('note-result').textContent='';fail('note-error',error);}finally{$('note-draft').disabled=false;}};
$('refresh-notes').onclick=()=>void loadNotes();
$('refresh-insights').onclick=()=>void loadInsights();setTabs();void loadReadiness();void load();void loadInsights();void loadNotes();
</script></body></html>`;
}
